{{- if .Values.celery.queues }}
{{- range .Values.celery.queues }}
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{ template "esgf-compute-wps.fullname" $ }}-celery-{{ .name }}
  labels:
    app: {{ template "esgf-compute-wps.name" $ }}-celery-{{ .name }}
    chart: {{ template "esgf-compute-wps.chart" $ }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
{{- if $.Values.celery.annotations }}
  annotations:
    {{ toYaml $.Values.celery.annotations | indent 2 }}
{{- end }}
spec:
  replicas: {{ .replicas }}
  selector:
    matchLabels:
      app: {{ template "esgf-compute-wps.name" $ }}-celery-{{ .name }}
      release: {{ $.Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "esgf-compute-wps.name" $ }}-celery-{{ .name }}
        release: {{ $.Release.Name }}
    spec:
      containers:
      - name: {{ template "esgf-compute-wps.name" $ }}-celery-{{ .name }}
        image: {{ $.Values.celery.image }}:{{ $.Values.celery.imageTag }}
        {{- if not $.Values.development }}
        args: 
          - "celery"
          - "worker"
          - "-A"
          - "compute"
          - "-l"
          - "INFO"
          - "--max-memory-per-child"
          - {{ $.Values.celery.resources.memory | quote }}
          - "-n"
          - {{ template "esgf-compute-wps.name" $ }}-celery-{{ .name }}
        {{- range $key, $value := .argsExtra }}
          - {{ $key | quote }}
          - {{ $value | quote }}
        {{- end }}
        {{- end }}
        {{- if $.Values.development }}
        command:
          - sleep
          - infinity
        {{- end }}
        resources:
          {{- if not $.Values.development }}
          limits:
            cpu: {{ $.Values.celery.resources.cpuLimit | quote }}
            memory: {{ $.Values.celery.resources.memoryLimit | quote }}
          {{- end }}
          requests:
            cpu: {{ $.Values.celery.resources.cpuRequest | quote }}
            memory: {{ $.Values.celery.resources.memoryRequest | quote }}
        env:
        {{- if $.Values.development }}
        - name: WPS_DEBUG
          value: "1"
        {{- end }}
        - name: WPS_HOST
          valueFrom:
            configMapKeyRef:
              name: {{ template "esgf-compute-wps.fullname" $ }}-app-config
              key: WPS_HOST
        - name: CELERY_BROKER
          valueFrom:
            configMapKeyRef:
              name: {{ template "esgf-compute-wps.fullname" $ }}-app-config
              key: CELERY_BROKER
        - name: CELERY_BACKEND
          valueFrom:
            configMapKeyRef:
              name: {{ template "esgf-compute-wps.fullname" $ }}-app-config
              key: CELERY_BACKEND
        - name: POSTGRES_HOST
          value: {{ template "postgresql.fullname" $ }}
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "esgf-compute-wps.fullname" $ }}-app-secret
              key: postgres_password
        volumeMounts:
        {{- if $.Values.development }}
        - mountPath: /var/www
          name: shared-volume
        {{- end }}
        - mountPath: /etc/config
          name: config-volume
        - mountPath: /data/public
          name: public-volume
        - mountPath: /data/cache
          name: cache-volume
        - mountPath: /data/ingress
          name: ingress-volume
      volumes:
      {{- if $.Values.development }}
      - name: shared-volume
        hostPath:
          path: /opt/cwt/data/shared
      {{- end }}
      - name: config-volume
        configMap:
            name: {{ template "esgf-compute-wps.fullname" $ }}-django-config
      - name: public-volume
      {{- if not $.Values.development }}
        persistentVolumeClaim:
          claimName: {{ template "esgf-compute-wps.name" $ }}-public-pvc
      {{- else }}
        hostPath:
          path: /opt/cwt/data/public
      {{- end }}
      - name: cache-volume
      {{- if not $.Values.development }}
        persistentVolumeClaim:
          claimName: {{ template "esgf-compute-wps.name" $ }}-cache-pvc
      {{- else }}
        hostPath:
          path: /opt/cwt/data/cache
      {{- end }}
      - name: ingress-volume
      {{- if not $.Values.development }}
        persistentVolumeClaim:
          claimName: {{ template "esgf-compute-wps.name" $ }}-ingress-pvc
      {{- else }}
        hostPath:
          path: /opt/cwt/data/ingress
      {{- end }}
---
{{- end }}
{{- end }}
