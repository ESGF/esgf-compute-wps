apiVersion: v1
kind: Service
metadata:
  name: {{ template "esgf-compute-wps.fullname" . }}-thredds
  labels:
    app: {{ template "esgf-compute-wps.fullname" . }}-thredds
    chart: {{ template "esgf-compute-wps.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selector:
    app: {{ template "esgf-compute-wps.fullname" . }}-thredds
    release: {{ .Release.Name }}
  ports:
  - port: 8080
