apiVersion: v1
kind: Secret
metadata:
  name: {{ template "esgf-compute-wps.fullname" . }}-app-secret
  labels:
    app: {{ template "esgf-compute-wps.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  oauth_client: {{ .Values.wps.oauth.client | default "" }}
  oauth_secret: {{ .Values.wps.oauth.secret | default "" }}
  postgres_password: {{ .Values.postgresql.postgresPassword }}
