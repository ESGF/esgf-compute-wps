apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "esgf-compute-wps.fullname" . }}-dask
  labels:
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/name: {{ template "esgf-compute-wps.name" $ }}
    helm.sh/chart: {{ $.Chart.Name }}-{{ $.Chart.Version }}
{{- if .Values.dask.scheduler.ingress.labels }}
{{ toYaml .Values.dask.scheduler.ingress.labels | nindent 4 }}
{{- end }}
{{- if .Values.dask.scheduler.ingress.annotations }}
  annotations:
{{ toYaml .Values.dask.scheduler.ingress.annotations | nindent 4 }}
{{- end }}
spec:
  rules:
  - http:
      paths:
      - path: /daskboard
        backend:
          serviceName: {{ template "esgf-compute-wps.fullname" . }}-dask-scheduler
          servicePort: 8787
